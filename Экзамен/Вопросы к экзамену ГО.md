# ЭКЗАМЕНАЦИОННЫЕ ВОПРОСЫ ПО ДИСЦИПЛИНЕ «ГЛУБОКОЕ ОБУЧЕНИЕ»

---

## Модуль 1. Основы глубокого обучения
*(Оцениваемые индикаторы: DL-1.1, DL-1.2, MF-3.1. Уровень средний)*

1. Предпосылки возникновения и основные достижения технологии глубокого обучения.
2. Обучающая, валидационная и тестовая выборки. Кросс-валидация и выбор гиперпараметров.
3. Мультиклассовая функция потерь SVM: формула, логика и пример реализации на Python.
4. Softmax-классификатор и кросс-энтропия. Пример кода на Python.
5. Вычислительный граф и цепное правило. Обратное распространение ошибки.
6. Шаблоны обратного потока градиентов. Обратное распространение при векторных вычислениях.
7. Функции активации: сигмоида, гиперболический тангенс, ReLU, Leaky ReLU, ELU, Maxout.
8. Инициализация весов: проблемы и методы.
9. Пакетная нормализация (Batch Normalization): назначение, псевдокод алгоритма.
10. Нормализация на слое (Layer Norm), на экземпляре (Instance Norm), на группе (Group Norm).
11. Регуляризация Dropout: принцип работы, инвертированный Dropout, пример реализации на Python.
12. Алгоритм SGD с моментом инерции и моментом Нестерова.
13. Адаптивные алгоритмы оптимизации: AdaGrad и RMSProp.
14. Алгоритм оптимизации Adam: принцип и преимущества.

---

## Модуль 2. Основы свёрточных нейронных сетей
*(Оцениваемые индикаторы: DL-1.4, MF-3.1, PL-1.2. Уровень средний)*

21. Свёрточный слой (CONV): рецептивное поле, фильтры, шаг, дополнение нулями. Расчёт размеров выходных карт.
22. Пулинг слой (POOL): назначение, max пулинг, ave пулинг, расчёт выходных размеров.
23. Свёртка 1×1: назначение и использование в архитектурах типа Inception.
24. Транспонированная свёртка: принцип работы и применение в сегментации.
25. Базовые архитектуры свёрточных сетей: LeNet, AlexNet.
26. Архитектура VGGNet: особенности и глубина.
27. Архитектура GoogLeNet / Inception: принцип построения блоков.
28. Архитектура ResNet: остаточные блоки и решение проблемы затухающего градиента.
29. Архитектура ResNeXt: отличие от ResNet.
30. Архитектура ZFNet: ключевые улучшения по сравнению с AlexNet.

---

## Модуль 3. Инструментарий глубокого обучения (PyTorch, TensorFlow, Keras)
*(Оцениваемые индикаторы: PL-1.2. Уровень средний)*

35. PyTorch: тензоры и их свойства. Автоматическое дифференцирование (Autograd).
36. PyTorch: модуль nn, модуль optim. Определение новых модулей.
37. PyTorch: использование контейнеров (Sequential, ModuleList), DataLoader.
38. TensorFlow: тензоры, переменные, константы. Вычислительный граф.
39. TensorFlow: модули train (оптимизация), losses, layers. Примеры использования.
40. TensorFlow: создание статического графа через @tf.function.
41. TensorFlow: GradientTape и его роль в автоматическом дифференцировании.
42. Keras: назначение и архитектура программно-аппаратного стека поддержки DL.
43. Keras: последовательная модель (Sequential API).
44. Keras: функциональный API и создание моделей через наследование от tf.keras.Model.

---

## Модуль 4. Современные архитектуры и применение CNN
*(Оцениваемые индикаторы: DL-1.3, DL-1.4, PL-1.2. Уровень средний)*

47. Передача обучения (Transfer Learning) и тонкая настройка (Fine-tuning).
48. Семейство R-CNN: эволюция от R-CNN к Faster R-CNN.
49. Одноступенчатые детекторы: YOLO и RetinaNet. Сравнение подходов.
50. Focal Loss: назначение и применение в RetinaNet.
51. Семантическая сегментация: постановка задачи и архитектура U‑Net.
52. Визуализация свёрточных сетей: карты значимости (Grad‑CAM, активационные карты).
53. Визуализация признаков CNN: градиентное восхождение по признакам слоя.
54. Преобразование стиля (Neural Style Transfer): потери контента и потери стиля.
55. Быстрое преобразование стиля: архитектура системы. Условная нормализация на экземпляре.

---

## Модуль 5. Рекуррентные нейронные сети (RNN)
*(Оцениваемые индикаторы: DL-1.5, PL-1.2. Уровень средний)*

61. Простая рекуррентная нейронная сеть: структура, реализация на Python.
62. Вычислительные графы RNN: виды развёрток (N:N, N:1, 1:N, N:1+1:N).
63. Прямое и обратное распространение во времени (BPTT).
64. Проблема затухающих и взрывных градиентов в RNN: причины и методы борьбы.
65. Архитектура LSTM: устройство вентилей (входной, забывающий, выходной).
66. Управляемый рекуррентный блок (GRU): отличия от LSTM, преимущества.
67. Двунаправленные RNN и многослойные RNN: принципы построения.
68. Моделирование языка на уровне символов с помощью RNN: кодирование, обучение, тестирование.
69. Формирование заголовков к изображениям с помощью RNN/LSTM: архитектура сети.
70. Формирование заголовков с фокусировкой внимания: механизм внимания в LSTM.

---

## Модуль 6. Трансформеры
*(Оцениваемые индикаторы: DL-1.9. Уровень базовый)*

71. Обобщённый слой внимания: векторы запросов, ключей, значений.
72. Слой самовнимания (Self-Attention): вычисление, интерпретация.
73. Позиционное кодирование: необходимость и способы реализации.
74. Причинно-следственная маскировка в трансформерах: назначение.
75. Многоголовое (многопоточное) самовнимание: архитектура и преимущества.
76. Блок кодера трансформера: структура, остаточные связи, нормализация.
77. Блок декодера трансформера: отличие от кодера.
78. Стандартный трансформер изображений на патчах: принцип работы.
79. Vision Transformer (ViT): архитектура и сравнение с CNN.
80. Модели BERT и GPT: сравнение архитектур, задач предобучения и сценариев применения.
81. Использование библиотеки Hugging Face Transformers: загрузка и настройка моделей.

---

## Рекомендации по формированию экзаменационных билетов

- Билет содержит **2 вопроса**.
- Рекомендуемое распределение: первый вопрос из модулей 1–3 (базовые принципы, инструментарий), второй — из модулей 4–6 (архитектуры, RNN, трансформеры).
- При необходимости преподаватель может варьировать сочетание модулей для проверки разных индикаторов.