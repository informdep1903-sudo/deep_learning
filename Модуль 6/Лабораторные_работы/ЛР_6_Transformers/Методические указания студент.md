**МЕТОДИЧЕСКИЕ УКАЗАНИЯ ДЛЯ СТУДЕНТА**

**1. Необходимые знания**
- Python (базовый), PyTorch (тензоры, `nn.Module`, автоматическое дифференцирование).
- Основы  механизма Attention.
- Понимание архитектуры Transformer (из лекций).
- Базовые навыки работы с Jupyter Notebook.

**2. Порядок выполнения**
(Ниже указан краткий порядок выполнения лабораторной работы. Развернутое описание с необходиммыми теоретическими сведениями приведено в самом блокноте)

Шаг 1. Проверка или установка окружения

Убедитесь, что у вас установлены необходимые библиотеки:

`pip install torch numpy pandas matplotlib scikit-learn`

Для выполнения лабораторной работы используйте заготовку шаблона кода в файле `Transformer_Captioning.ipynb`.

Шаг 2. Изучение теории и структуры задания
Прочитайте описание архитектуры Transformer во вступлении к блокноту. Обратите особое внимание на:

-	Механизм "Scaled Dot-Product Attention".
-	"Multi-Head Attention".
-	"Positional Encoding".

Шаг 3. Реализация слоя MultiHeadAttention
1.	Откройте файл `dlcv/transformer_layers.py`.
2.	Найдите класс MultiHeadAttention.
3.	Реализуйте метод `forward` согласно описанию в комментариях к коду и формуле:

Attention(Q, K, V) = softmax(QK^T / sqrt(d_k)) V


4.	Убедитесь, что вы правильно разделили входные данные на `num_heads` и выполнили линейные преобразования.
5.	Запустите ячейку с проверкой реализации. Относительная ошибка должна быть порядка e-3 или менее.

Шаг 4. Реализация слоя `PositionalEncoding`
1.	В том же файле `dlcv/transformer_layers.py` найдите класс `PositionalEncoding`.
2.	Реализуйте функцию генерации позиционного кодирования согласно формулам:

- PE_(i, j) = sin(i / 10000^(j/d)), j -четное
- PE_(i, j) = cos(i / 10000^(j-1/d)), иначе

3.	Запустите ячейку с проверкой реализации.

Шаг 5. Реализация модели `CaptioningTransformer`
1.	Откройте файл `dlcv/classifiers/transformer.py`.
2.	Найдите класс `CaptioningTransformer` и метод `forward`.
3.	Реализуйте прямой проход модели согласно архитектуре, описанной в комментариях :
-	Встраивание слов (word_embedding).
-	Добавление позиционного кодирования к встраиваниям.
-	Проход через стопку слоев декодера Transformer (блоки self-attention и cross-attention).
-	Применение финального линейного слоя для получения логитов.
4.	Запустите ячейку с проверкой прямого прохода модели. Относительная ошибка должна быть порядка e-5 или менее.

Шаг 6. Обучение модели
1.	Настройте гиперпараметры в ячейке, создающей экземпляр `CaptioningSolverTransformer`.
2.	Запустите процесс обучения. Отслеживайте динамику потерь.
3.	Постройте график потерь.

Шаг 7. Тестирование и визуализация
1.	Запустите ячейку с кодом тестирования.
2.	Визуализируйте изображения вместе со сгенерированными и эталонными подписями.
3.	Проанализируйте, насколько хорошо модель справляется с задачей.

Шаг 8. Анализ и выводы
1.	Сравните качество работы Transformer с RNN/LSTM .
2.	Ответьте на вопросы: Что получается у модели хорошо? Какие ошибки она допускает?

Шаг 9. Дополнительные задания (по желанию)
1.	Попробуйте изменить количество голов внимания (num_heads) и посмотрите, как это повлияет на обучение и качество.
2.	Попробуйте изменить количество слоев (num_layers) и оцените изменения.

Шаг 10. Оформление отчёта
1.	Сохраните ваш Jupyter Notebook.
2.	Подготовьте отчёт (PDF или Markdown), включив в него все необходимые разделы (см. "Требования к отчёту").
